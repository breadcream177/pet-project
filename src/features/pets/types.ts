export type Pet = {
  id: string;
  name: string;
  species: string;
  color: string;
  memo: string;
};

export type PetFormValues = {
  name: string;
  species: string;
  customSpecies: string;
  color: string;
  memo: string;
};
